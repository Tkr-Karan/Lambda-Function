def add(x,y):
    return x + y

# here we use the lambda function instead of defining th function
# You also called this the one line code.

add = lambda x , y: x+y

print(add(2,5))

print(add(2, 5))
